import { NestablelistDirective } from './nestablelist.directive';

describe('NestablelistDirective', () => {
  it('should create an instance', () => {
    const directive = new NestablelistDirective();
    expect(directive).toBeTruthy();
  });
});
