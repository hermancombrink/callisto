@using Callisto.Web.Client.Extensions;

<nav class="navbar-default navbar-static-side" role="navigation">
    <div class="sidebar-collapse">
        <ul class="nav" id="side-menu">
            <li class="nav-header">
                <div class="dropdown profile-element">
                    <span>
                        <a href="@Url.Action("Index", "Home")">   <img alt="image" class="img-circle" src="~/Images/profile_small.jpg" />  </a>
                    </span>
                    <a data-toggle="dropdown" class="dropdown-toggle" href="#">
                        <span class="clear">
                            <span class="block m-t-xs">

                                <strong class="font-bold">@User.Identity.Name</strong>

                            </span> <span class="text-muted text-xs block">Demo User <b class="caret"></b></span>
                        </span>
                    </a>
                    <ul class="dropdown-menu animated fadeInRight m-t-xs">
                        <li><a href="@Url.Action("Index", "Manage")">Profile</a></li>
                        <li><a href="@Url.Action("Inbox", "Mailbox")">Mailbox</a></li>
                        <li class="divider"></li>

                        <li>
                            <form asp-controller="Account" asp-action="Logout">
                                <button type="submit" class="btn btn-link">
                                    <i class="fa fa-sign-out"></i> Log out
                                </button>
                            </form>
                        </li>
                    </ul>
                </div>
                <div class="logo-element">
                    C+
                </div>
            </li>
            <li class="@Html.IsSelected(controller: "Home")">
                <a href="@Url.Action("Index", "Home")"><i class="fa fa-th-large"></i> <span class="nav-label">Main</span> <span class="label label-primary pull-right"></span></a>
            </li>
            <li class="@Html.IsSelected(controller: "Assets")">
                <a href="#"><i class="fa fa-building"></i> <span class="nav-label">Assets</span><span class="fa arrow"></span></a>
                <ul class="nav nav-second-level collapse @Html.IsSelected(controller: "Assets", cssClass: "in")">
                    <li class="@Html.IsSelected(action: "Flot")"><a href="@Url.Action("Index", "Assets")">Search</a></li>
                    <li class="@Html.IsSelected(action: "Morris")"><a href="@Url.Action("Create", "Assets")">Create</a></li>
                </ul>
            </li>
            <li class="@Html.IsSelected(controller: "WorkOrders")">
                <a href="#"><i class="fa fa-clipboard"></i> <span class="nav-label">Work Orders</span><span class="fa arrow"></span></a>
                <ul class="nav nav-second-level collapse @Html.IsSelected(controller: "WorkOrders", cssClass: "in")">
                    <li class="@Html.IsSelected(action: "Flot")"><a href="@Url.Action("Index", "WorkOrders")">Search</a></li>
                    <li class="@Html.IsSelected(action: "Morris")"><a href="@Url.Action("Create", "WorkOrders")">Create</a></li>
                </ul>
            </li>
        </ul>
    </div>
</nav>